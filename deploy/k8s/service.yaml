apiVersion: v1
kind: Service
metadata:
  name: aegis-web
  namespace: aegis
  labels:
    app: aegis-web
spec:
  type: ClusterIP
  selector:
    app: aegis-web
  ports:
    - name: http
      port: 80
      targetPort: http
